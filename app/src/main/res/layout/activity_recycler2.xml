<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFDD0"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center_horizontal">

    <!-- Title -->
    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Practice Sellect"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#FF486C"
        android:layout_marginBottom="24dp"/>

    <!-- RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/challengeRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#FFFFFF"
        android:padding="12dp"
        android:elevation="2dp"
        android:clipToPadding="false"
        android:layout_marginBottom="24dp"
        android:layout_marginTop="8dp"/>

    <!-- Practice Speed Setting Title -->
    <TextView
        android:id="@+id/tvPracticeSpeedSetting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="연습 속도 설정"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#FF486C"
        android:layout_marginBottom="8dp" />

    <!-- RadioGroup for Practice Speed -->
    <RadioGroup
        android:id="@+id/rgPracticeSpeed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="24dp">

        <RadioButton
            android:id="@+id/rb0_25x"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0.25x" />

        <RadioButton
            android:id="@+id/rb0_5x"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0.5x" />

        <RadioButton
            android:id="@+id/rb0_75x"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0.75x" />

        <RadioButton
            android:id="@+id/rb1_0x"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1.0x" />
    </RadioGroup>

    <!-- Confirm Button -->
    <Button
        android:id="@+id/btnConfirm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="뒤로가기"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:backgroundTint="#FF6699"
        android:paddingLeft="32dp"
        android:paddingRight="32dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:elevation="4dp"/>
</LinearLayout>