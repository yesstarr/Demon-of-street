<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Exercises_FitFuel"
        tools:targetApi="31">
        <activity
            android:name="com.ooplab.exercises_fitfuel.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.ooplab.exercises_fitfuel.ResultActivity" />

        <activity android:name="com.ooplab.exercises_fitfuel.LoginActivity" />

        <activity android:name="com.ooplab.exercises_fitfuel.RecyclerActivity" />

        <activity android:name="com.ooplab.exercises_fitfuel.Recycler2Activity" />

        <activity android:name="com.ooplab.exercises_fitfuel.SignupActivity" />

        <activity android:name="com.ooplab.exercises_fitfuel.MyPageActivity" />

        <activity android:name="com.ooplab.exercises_fitfuel.MainScreenActivity" />

        <activity android:name="com.ooplab.exercises_fitfuel.MainActivity" />

        <activity android:name="com.ooplab.exercises_fitfuel.SettingsActivity" />

        <activity android:name=".VideoPlayerActivity" />

        <activity android:name=".LikedVideosActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity
            android:name=".LeaderboardActivity"
            android:exported="false" />

    </application>

</manifest>